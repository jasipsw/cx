# Global temperature conversion functions – available everywhere
- name: temp_conversion_macros
  unique_id: temp_conversion_macros
  state: >
    {{ "" }}   # dummy state, we only care about the attributes
  attributes:
    celsius: >
      {% from 'init.jinja' import celsius %}
      {{ celsius }}
    fahrenheit: >
      {% from 'init.jinja' import fahrenheit %}
      {{ fahrenheit }}