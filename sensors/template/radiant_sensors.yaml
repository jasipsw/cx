- sensor:
  - name: "Master Suite Radiant Flow Temp"
    unique_id: "flow_temp_master_suite_radiant"
    unit_of_measurement: "°F"
    state_class: measurement
    icon: mdi:thermometer
    availability: >-
      {{ is_state('binary_sensor.master_suite_radiant', 'on') }}
    state: >-
      {# Using buffer supply temp (hot water from HP to radiant floor) #}
      {{ states('sensor.buffer_supply_from_heat_pump') | float(0) }}
